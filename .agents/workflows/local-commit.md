---
description: Stage all changes and commit locally with an auto-generated message (local only, no remote push)
---

// turbo-all

1. Check repository status to see what's changed
   `git status`

2. Stage all modifications in the current repository
   `git add .`

3. Automatically generate a concise, conventional commit message based on the changes and commit locally. NEVER ask for confirmation.
   `git commit -m "<auto-generated message>"`
